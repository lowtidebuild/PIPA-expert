---
description: Audit a markdown file with citation-auditor and return annotated markdown.
argument-hint: "<file.md>"
disable-model-invocation: true
---

Use the `citation-auditor` skill with the file path provided in `$ARGUMENTS`.

If no path is provided, ask for a markdown file path.

Pass the path through unchanged and return the skill's final annotated markdown output.
